{
  "name": "hello-world-target",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test 'tests/**/*.test.*'"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20"
  }
}
